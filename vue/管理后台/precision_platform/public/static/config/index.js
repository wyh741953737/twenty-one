// const origin = window.location.origin + '/'
const origin = 'http://192.168.110.73:8080/'

const baseURL = origin + 'marketing/'
const uploadURL = origin + 'upload'
const ueditUploadURL = origin + 'upload/uploadBackPicture'

window.API_CONFIG = {
  baseURL,
  uploadURL,
  ueditUploadURL
}
