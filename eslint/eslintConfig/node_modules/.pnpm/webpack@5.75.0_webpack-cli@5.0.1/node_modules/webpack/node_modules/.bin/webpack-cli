#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/mnt/e/wyhstudy/twenty-one/eslint/eslintConfig/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/mnt/e/wyhstudy/twenty-one/eslint/eslintConfig/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../webpack-cli@5.0.1_webpack@5.75.0/node_modules/webpack-cli/bin/cli.js" "$@"
else
  exec node  "$basedir/../../../../../webpack-cli@5.0.1_webpack@5.75.0/node_modules/webpack-cli/bin/cli.js" "$@"
fi
